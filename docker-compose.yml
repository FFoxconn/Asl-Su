services:
  sqlserver:
    image: mcr.microsoft.com/mssql/server:2022-latest
    container_name: aslsu-sqlserver
    environment:
      ACCEPT_EULA: "Y"
      MSSQL_SA_PASSWORD: "${MSSQL_SA_PASSWORD:?set MSSQL_SA_PASSWORD in your shell or a local .env file, never commit it}"
      MSSQL_PID: "Developer"
    ports:
      - "1433:1433"
    volumes:
      - aslsu-sqlserver-data:/var/opt/mssql
    healthcheck:
      test: ["CMD-SHELL", "/opt/mssql-tools18/bin/sqlcmd -S localhost -U sa -P \"$$MSSQL_SA_PASSWORD\" -C -Q 'SELECT 1' || exit 1"]
      interval: 10s
      timeout: 5s
      retries: 10

volumes:
  aslsu-sqlserver-data:
